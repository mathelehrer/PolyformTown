# Flags and Parameters

This appendix will track command-line options after naming cleanup.

Priority items:

- replace positional magic cutoffs with named flags
- document defaults and rationale
- align `-h` text with method-first naming
